'use client';

/**
 * DEPRECATED: Este componente ya no se usa
 * La autenticación ahora se maneja en /auth/redirect
 * Mantenido solo para compatibilidad con código legacy
 */
export default function AuthRedirectHandler() {
  return null;
}
